version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fisioterapeuta-web
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    # Si necesitas variables de entorno adicionales, descomenta y añádelas:
    # env_file:
    #   - .env.local
